async function easteregg() {
    const lol = document.getElementById("notify")
    let rnd = Math.floor(Math.random() * 11); // 0 to 10
    if (rnd === 0) {
        console.log("You found the easter egg!");
        window.location = 'llve.html';
    } else {

    }
}